[project]
name = "djangotutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "djlint>=1.44.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
